<!-- Carte 1 : Gestion du script Python -->
<p class="lead">Choisissez un script à exécuter ou à éditer et saisissez votre jeton.</p>

<div class="row align-items-end mb-4">
    <!-- Sélecteur de scripts pour Base64 -->
    <div class="col-8">
        <select class="form-select" id="script-select-base64">
            {% for script in scripts_base64 %}
                <option value="{{ script }}">{{ script }}</option>
            {% endfor %}
        </select>
    </div>

    <!-- Input pour le jeton -->
    <div class="col-8 mt-3">
        <input type="text" class="form-control" id="token-base64-input" placeholder="Entrez votre jeton" />
    </div>

    <!-- Boutons -->
    <div class="col-4">
        <div class="d-flex justify-content-center">
            <button class="btn btn-primary me-2" id="run-script-base64-btn" title="Exécuter le script">
                <i class="fa-solid fa-play"></i>
            </button>
            <button class="btn btn-secondary me-2" id="edit-script-base64-btn" data-bs-toggle="modal" data-bs-target="#editScriptBase64Modal" title="Éditer le script">
                <i class="fa-solid fa-pen-to-square"></i>
            </button>
        </div>
    </div>
</div>

<h3>Sortie du script :</h3>
<div id="script-output-base64" class="alert alert-secondary" role="alert">
    <em>Le script n'a pas encore été exécuté.</em>
</div>


<!-- Modals -->
{% include 'home/fragments/programmation/base64/modals/edit_script_modal.html.twig' %}
{% include 'home/fragments/programmation/base64/modals/success_modal.html.twig' %}

<script src="{{ asset('js/home/base64/run.js') }}"></script>
<script src="{{ asset('js/home/base64/edit.js') }}"></script>
<script src="{{ asset('js/home/base64/save.js') }}"></script>
